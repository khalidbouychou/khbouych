apiVersion: apps/v1
kind: Deployment
metadata:
  name: wil-playground
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wil-playground
  template:
    metadata:
      labels:
        app: wil-playground
    spec:
      containers:
      - name: playground-container
        image: wil42/playground:v1 # <--- During defense, you will change this to v2
        ports:
        - containerPort: 8888
---
apiVersion: v1
kind: Service
metadata:
  name: wil-playground-service
  namespace: dev
spec:
  selector:
    app: wil-playground
  ports:
    - protocol: TCP
      port: 8888        # Port exposed by the Service
      targetPort: 8888  # Port the app is listening on inside the container
  type: LoadBalancer    # This allows K3d to map it to your iMac's port 8888
